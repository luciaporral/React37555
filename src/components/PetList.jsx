import React from "react";
import Pet from "./Pet";

const PetList =(props) =>{


    return(
        <div>PetList</div>
 /*       <div>
            {props.data.map((item) => {
                return(
                    <Item
                        key={item.id}
                        price={item.price}
                        title={item.title}
                        description={item.description}
                        />
                )
            })}
        </div>
        */
    )
}; export default PetList